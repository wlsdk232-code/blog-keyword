# 네이버 API 키 — 절대 깃허브에 올라가면 안 됨
.env

# 구글 서비스 계정 키(JSON) — 절대 깃허브에 올라가면 안 됨
blog-search-stats-*.json
*service-account*.json
*-gcp-*.json

# 파이썬 캐시
__pycache__/
*.pyc

# macOS
.DS_Store

# Claude 설정 폴더
.claude/
